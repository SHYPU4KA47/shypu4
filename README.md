# Приложение для поиска книг в OpenLibrary

Консольное и графическое приложение на Java для поиска книг через OpenLibrary API.

## Описание

Приложение позволяет искать книги в онлайн-библиотеке OpenLibrary по названию или автору. Результаты поиска отображают название книги, автора(ов) и год первой публикации.

## Возможности

- 🔍 Поиск книг по названию или автору
- 📚 Отображение информации о книгах (название, автор, год издания)
- 🖥️ Консольный интерфейс
- 🎨 Графический интерфейс на JavaFX
- 📝 Логирование работы приложения (консоль и файл)
- ✅ Модульное тестирование с JUnit
- 📖 Автоматическая генерация JavaDoc

## Технологии

- **Java 17** - язык программирования
- **Jackson** - обработка JSON-данных от API
- **Log4j2** - логирование работы приложения
- **JUnit 5** - модульное тестирование
- **Gradle** - система сборки проекта
- **Maven** - альтернативная система сборки
- **JavaFX** - графический интерфейс пользователя

## Структура проекта

```
API/
├── src/
│   ├── main/
│   │   ├── java/app/
│   │   │   ├── OpenLibraryClient.java    # Основной класс приложения
│   │   │   └── OpenLibraryFxApp.java      # Графический интерфейс
│   │   └── resources/
│   │       └── log4j2.xml                 # Конфигурация логирования
│   └── test/
│       └── java/app/
│           └── OpenLibraryClientTest.java # Модульные тесты
├── build.gradle                          # Конфигурация Gradle
├── pom.xml                               # Конфигурация Maven
├── settings.gradle                       # Настройки Gradle
└── .gitignore                            # Игнорируемые файлы Git
```

## Требования

- JDK 17 или выше
- Gradle 7.0+ (или Maven 3.6+)
- Доступ в интернет (для работы с OpenLibrary API)

## Установка и сборка

### Клонирование репозитория

```bash
git clone https://github.com/SHYPU4KA47/shypu4.git
cd shypu4
```

### Сборка проекта с Gradle

```bash
gradle build
```

### Сборка проекта с Maven

```bash
mvn clean package
```

## Использование

### Консольное приложение

#### Запуск через Gradle

1. Интерактивный режим (запрос ввода):
   ```bash
   gradle run
   ```
   Введите поисковый запрос при запросе.

2. С аргументами командной строки:
   ```bash
   gradle run --args="Толкин"
   ```

#### Запуск через Maven

1. Сборка JAR-файла:
   ```bash
   mvn package
   ```

2. Запуск JAR-файла:
   ```bash
   java -jar target/online-library-client-1.0.0.jar "Толкин"
   ```

### Графический интерфейс

#### Запуск через Gradle

```bash
gradle run -PmainClass=app.OpenLibraryFxApp
```

#### Использование GUI

1. Запустите приложение командой выше
2. Введите поисковый запрос в поле ввода (например: "Толкин" или "Гарри Поттер")
3. Нажмите кнопку "Искать" или клавишу Enter
4. Результаты отобразятся в списке ниже

## Примеры использования

### Поиск по автору

```
Запрос: Толкин
Результат:
Показаны первые 5 результатов по запросу: Толкин
The Fellowship of the Ring — J. R. R. Tolkien (1954)
The Two Towers — J. R. R. Tolkien (1954)
The Return of the King — J. R. R. Tolkien (1955)
...
```

### Поиск по названию

```
Запрос: Гарри Поттер
Результат:
Показаны первые 5 результатов по запросу: Гарри Поттер
Harry Potter and the Philosopher's Stone — J. K. Rowling (1997)
Harry Potter and the Chamber of Secrets — J. K. Rowling (1998)
...
```

## Логирование

Приложение использует Log4j2 для логирования. Логи сохраняются в:

- **Консоль** - уровень INFO и выше
- **Файл** `logs/app.log` - все уровни логирования

Конфигурация логирования находится в `src/main/resources/log4j2.xml`.

## Тестирование

### Запуск тестов через Gradle

```bash
gradle test
```

### Запуск тестов через Maven

```bash
mvn test
```

Тесты проверяют корректность форматирования результатов поиска.

## Генерация документации

### JavaDoc через Gradle

```bash
gradle javadoc
```

Документация будет доступна в `build/docs/javadoc/index.html`

### JavaDoc через Maven

```bash
mvn javadoc:javadoc
```

Документация будет доступна в `target/site/apidocs/index.html`

## API

Приложение использует публичный REST API OpenLibrary:

- **Документация API**: https://openlibrary.org/developers/api
- **Endpoint поиска**: https://openlibrary.org/search.json
- **Формат запроса**: `GET https://openlibrary.org/search.json?q={запрос}`

## Обработка ошибок

Приложение обрабатывает следующие ситуации:

- ✅ Пустой поисковый запрос
- ✅ Отсутствие результатов поиска
- ✅ Ошибки сети (таймауты, недоступность API)
- ✅ Некорректные ответы от API
- ✅ Отсутствие стандартного ввода (при запуске через Gradle)

Все ошибки логируются в файл и выводятся пользователю в понятном формате.

## Разработка

### Основные классы

- **OpenLibraryClient** - основной класс, выполняющий HTTP-запросы и обработку данных
- **OpenLibraryFxApp** - графический интерфейс на JavaFX
- **OpenLibraryClientTest** - модульные тесты

### Зависимости

Проект использует следующие библиотеки:

- `jackson-databind` - парсинг JSON-ответов от API
- `log4j-api`, `log4j-core`, `log4j-slf4j2-impl` - логирование
- `junit-jupiter` - модульное тестирование
- `javafx-controls` - графический интерфейс

## Лицензия

Этот проект создан в учебных целях.

## Автор

SHYPU4KA47

## Ссылки

- [OpenLibrary API Documentation](https://openlibrary.org/developers/api)
- [GitHub Repository](https://github.com/SHYPU4KA47/shypu4)

